{
  "name": "fixnow-backend",
  "version": "1.0.0",
  "description": "Backend for FixNow App",
  "main": "db.json",
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
